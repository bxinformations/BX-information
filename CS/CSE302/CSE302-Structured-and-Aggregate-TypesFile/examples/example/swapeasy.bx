def swap(x : int*, y : int*)
{
    var tmp = *x : int;
   // print(tmp);
    *x = *y;
   // print(*y);
    *y = tmp;
    //print(*y);
}

def main() 
{
    var x = 1, y = 2 : int;
    swap(&x, &y);
    print(x);
    print(y);
}